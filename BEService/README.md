# BE

Node.js Express backend service.

## Setup

```bash
npm install
```

## Run

```bash
npm run dev
```

The API runs on `http://localhost:3000` by default.

Available endpoints:

- `GET /`
- `GET /health`
